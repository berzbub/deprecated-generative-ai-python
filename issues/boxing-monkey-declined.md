# Issue: "Boxing monkey" — Declined (Ambiguous)

## Status: No action taken

The problem statement `Boxing monkey` does not specify:

- Which module, class, or function is affected
- What the expected vs. actual behaviour is
- What a passing acceptance test would look like

This is a legacy repository that accepts **critical bug fixes only**. Without a concrete,
actionable requirement, no changes can be safely made.

## Resolution

Please reopen with a specific description of:
1. The failing behaviour or bug (include a minimal reproducer if possible)
2. The expected outcome
3. The relevant source file(s) or API surface
